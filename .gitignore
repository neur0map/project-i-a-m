# Reference material (not for distribution)
reference/

# OS junk
.DS_Store
Thumbs.db

# Editor/IDE
*.swp
*.swo
*~
.vscode/
.idea/

# Secrets (just in case)
*.key
*.pem
*.env
credentials*
**/token*
**/hosts.yml

# gh CLI auth (never ship tokens)
shell/.config/gh/

# atuin session keys
shell/.config/atuin/key
shell/.config/atuin/session

# Fish variables (contains per-machine state)
shell/.config/fish/fish_variables
